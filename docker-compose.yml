services:
  propongo:
    build: .
    image: propongo:latest
    ports:
      - "5000:5000"
    volumes:
      # Persist proposals, templates, and exports on the host.
      - ./data:/app/data
    environment:
      - DEBUG=False
      - HOST=0.0.0.0
      - PORT=5000
    restart: unless-stopped
